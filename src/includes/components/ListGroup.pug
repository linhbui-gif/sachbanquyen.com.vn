mixin ListGroupComponent({isAccordion, name, className})
  .list-group(class=`${className ? className : ''}`)
    h3.list-group-item.header=name
    if !isAccordion
       each item in [1,2,3,4,5]
         a.list-group-item.list-group-item-action Hello
    else
      +AccordionComponent()
    a(href="#").list-group-item.text-center
       | Xem thêm
       i.fa.fa-angle-down
